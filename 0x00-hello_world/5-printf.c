#include <stdio.h>

/**
 * main - print "with proper grammar, but the outcome is
 * 		a peice of art," followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a peice of art,\n");

	return (0);
}
